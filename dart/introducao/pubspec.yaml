name: introducao
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  #versionamento semântico
  #major(quebra de compatibilidade), minor(novas funcionalidades mas continua compativel), patch(corrige bug)
  sdk: ^3.0.6

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
